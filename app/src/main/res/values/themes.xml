<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.BeadWrapper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Normal app surfaces -->
    <style name="Theme.BeadWrapper" parent="Base.Theme.BeadWrapper"/>

    <!-- ────────────────────────────────────────────────────────────────
         Full-screen variant used only by PaymentWebViewActivity
         (referenced in the manifest). Adds true full-screen + edge-to-edge.
    ──────────────────────────────────────────────────────────────── -->
    <style name="Theme.BeadWrapper.Fullscreen" parent="Theme.BeadWrapper">
        <!-- Hide status bar -->
        <item name="android:windowFullscreen">true</item>

        <!-- Already no action-bar because base theme = NoActionBar -->
        <!-- Optional: transparent status bar for seamless WebView -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
